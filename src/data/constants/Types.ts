export type User = {id: string, username: string, password?: string, email: string, firstName: string, lastName: string, active: 0 | 1, isAdmin: 0 | 1}

export type Player = {username: string, password: string, email: string, firstName: string, lastName: string}